import React from "react";
import { observer } from "mobx-react";

const ConnectionLostTemplate = observer((props) => {
    return <div>
        Connection Lost
    </div>
})

export default ConnectionLostTemplate;