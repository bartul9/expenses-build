import React from "react";
import { observer } from "mobx-react";


const FooterTemplate = observer((props) => {
    return (
        <footer className="footer"> 
            
        </footer>
    )
})

export default FooterTemplate;